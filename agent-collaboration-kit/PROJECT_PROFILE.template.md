# 项目档案

> **推荐**：将本文件复制到 **Git 仓库根目录**，命名为 **`PROJECT_PROFILE.md`**（与 `agent-collaboration-kit` 文件夹并列）。协议会优先读仓库根的这一份。  
> **代码 AI** 与 **Review AI** 除阅读协作包内 `.agents/README.md` 外，应阅读本文件（若存在）。

## 基本信息

- **项目一句话**：
- **项目类型**（选一或多）：`CLI` / `Library` / `Web` / `Mobile` / `Desktop` / `Plugin-for-<宿主名>` / `Other`
- **主分支 / 发布方式**：
- **主工作区路径约定**：（若固定，写明；避免多份拷贝不同步）
- **仓库布局**：`单仓` / `Monorepo`（若 monorepo，写明主要包路径）

## 运行时与语言

- **语言与最低版本**：例：Node 22+、Python 3.11+、Go 1.22+
- **包管理 / 锁文件**：例：`pnpm`、`npm`、`uv`
- **AI 引入新依赖**：`须用户每次确认` / `允许在任务卡记录前提下引入` / `禁止自动新增`

## 领域词汇（可选）

在任务、审查、日志中统一使用，避免歧义：

| 内部称呼 | 含义 |
|----------|------|
| 例：`后台` | 例：管理端 Web |
| 例：`客户端` | 例：iOS App |

## 源码与产物

- **主要源码路径**：例 `src/**`、`packages/app/**`
- **生成物 / 禁止手改**：例 `dist/**`、`*.generated.ts`
- **构建命令**：例 `npm run build`
- **类型检查 / 静态检查**：例 `npx tsc --noEmit`、`ruff check`
- **测试命令**：例 `npm test`
- **CI**：入口文件名或链接：例 `.github/workflows/ci.yml`；本地须跑的最小集合：

## 部署或同步（可选）

若存在「实现 ≠ 立刻上线」的第二步，写明：

- **动作名称**：例：同步到测试机、发版、上传 OSS
- **谁执行**：通常 **代码 AI** 仅在用户明确授权后执行
- **校验方式**：例：哈希对比、健康检查 URL

无此步骤可写：**无单独部署步骤；commit 即交付**。

## 严审触发（可覆盖默认）

默认：存储层、数据迁移、鉴权、支付、并发安全、移动端关键路径、或单次改动跨 **>3** 个业务文件时，须 **Review AI** 严格审查后再由用户授权 commit。

本项目追加：

- （在此列出）

## 设计文档位置（可选）

- **设计/方案笔记目录**：例 `docs/design/`（无则写 **无**）

## 其他约束

- （许可证、密钥、禁止提交的文件、`.gitignore` 中敏感模式）
