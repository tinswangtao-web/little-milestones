{
  "name": "kid-score",
  "version": "2.1.0",
  "description": "Daily record plugin for kids — behavior scoring, diary, diet & activity tracking",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "sync:vault": "node scripts/deploy.mjs --no-build",
    "deploy": "node scripts/deploy.mjs"
  },
  "keywords": [],
  "author": "Tins",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "^1.12.0",
    "typescript": "^5.0.0"
  }
}
