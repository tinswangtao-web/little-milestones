export interface ScoreCategoryLayoutRefs {
  section: HTMLElement;
  header: HTMLElement | null;
  titleHost: HTMLElement;
  addButtonHost: HTMLElement | null;
  grid: HTMLElement;
}

export interface CustomItemRowLayoutRefs {
  wrap: HTMLElement;
  row: HTMLElement;
  main: HTMLElement;
}
