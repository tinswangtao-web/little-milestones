export interface ScoreCardLayoutRefs {
  card: HTMLElement;
  content: HTMLElement;
  emoji: HTMLElement;
  name: HTMLElement;
  noteHost: HTMLElement;
  points: HTMLElement;
  yesterday: HTMLElement;
}
