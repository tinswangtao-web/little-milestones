export interface DiaryModuleRowLayoutRefs {
  row: HTMLElement;
  top: HTMLElement;
  main: HTMLElement;
  meta: HTMLElement;
  actions: HTMLElement;
  placeholderField: HTMLElement;
}

export interface ItemSettingsRowLayoutRefs {
  wrap: HTMLElement;
  row: HTMLElement;
  noteRow: HTMLElement;
}
