# iOS 移动端 UX 修复（打分页 + 设置页）

- **slug**: 2026-04-20-mobile-ux
- **owner**: claude-code
- **created**: 2026-04-20
- **status**: review-passed-with-notes
- **sync-to-vault**: done (latest: 2026-04-22 00:00, codex via primary workspace sync + hash verification)
- **active-worktree**: `/Users/tins-macmini/Documents/Vibe Coding/obsidian-little-milestones/.claude/worktrees/sleepy-engelbart-925e6b`

## Context / 用户需求

iPhone 16 Pro / iOS 26.4.1 上使用 Little Milestones 插件体验不佳。用户反馈 4 个阻塞性问题：
1. 打分页编辑项目时，点备注输入框被键盘覆盖，卡片不能上滑
2. 设置页新增打分项后页面跳到"分类管理"顶部，而非留在新增项处
3. 设置页可左右横滑，干扰上下滚动
4. 设置页滚动时误触输入框会弹键盘

## 受影响文件

- `main.js` — `setupMobileKeyboard` / `DailyScoringModal.showEditMode` / `KidScoreSettingTab.display` + `renderCategories` + `renderItems`
- `styles.css` — `.kid-score-value-popup` / 新增 `.kid-score-settings-root` / `.kid-score-new-highlight`
- `sync-push.sh`

## 禁止手改

本次因为 `main.ts` / esbuild 流水线尚未就位，**临时允许直接改 `main.js` 和 `styles.css`**。后续任务应优先补源码路径，再改这些产物视为违规。

## 实现摘要

1. 打分编辑弹窗：popup 加 `max-height`/`overflow-y: auto`；`setupMobileKeyboard` 随 `visualViewport` 压缩 popup 高度；`scrollIntoView` 改 `block:"center"`，延迟 350→60ms；移除移动端自动 focus
2. 设置页新增滚动：`renderCategories` / `renderItems` 保存并恢复 `containerEl.scrollTop`，新项加 `.kid-score-new-highlight` 动画并 `scrollIntoView`
3. 横向滑动锁定：新增 `.kid-score-settings-root` class，`overflow-x: hidden`，子元素 `flex-wrap: wrap; min-width: 0`
4. 滚动误触：`display()` 中给根容器绑 `touchstart/move/end`，位移 >8px 时给全体 input/textarea 加 `readonly`，`touchend` 后 250ms 解除
5. 继续收口 iPhone 键盘与设置页滚动：编辑卡片在键盘弹出时按可视区底边贴齐，不再只是估算上移；手动上下拖动改成不再使用固定 snap 点，允许把卡片拖到能看到最上方或最下方；设置页打分项列表重绘时先保住当前滚动位置，仅在新增项目时再滚到新项并聚焦

## 验收清单

- [ ] iPhone 打分页 → 编辑项目 → 点备注 → 键盘弹出后输入框可见，popup 可滚动
- [ ] 设置页"新增打分项" → 页面保持在原位，新项带高亮
- [ ] 设置页删除打分项 → 页面保持在当前编辑位置，不跳回列表开头
- [ ] 设置页手指横向滑 → 不再横移
- [ ] 设置页从输入框起竖向滑 → 键盘不弹
- [ ] Mac 桌面端无回归（自动 focus 只在非 mobile 保留）
- [ ] 打分页日记模块：长文本输入不再在小卡片里出现内部滚动条，天气/心情长内容能完整换行显示
- [ ] 设置页日记模块：提示文案输入框能展示长内容，新增/编辑模块时可设置 emoji，并在打分页同步显示
- [ ] 所有页面：不再出现硬编码的“小朋友”字样
- [x] Vault 同步完成（2026-04-22 00:00，codex 从主工作区同步并校验）

## 实施日志

- [2026-04-20 11:01 claude] 完成 4 项改动 → 执行 sync-push.sh → 已同步到 vault
- [2026-04-20 12:00 claude] 创建 `.agents/` 脚手架并把本任务登记进协议
- [2026-04-20 12:20 codex] 完成 review，结论为 pass-with-notes；同步 review 状态回主工作区 `.agents/`
- [2026-04-21 16:00 kimi] 完成 second review，发现 7 项问题（R1-R7），记录在 `.agents/reviews/2026-04-21-codex-refactor-review.md`
- [2026-04-21 16:30 kimi] 实现 R2-R7 修复：统一平台检测、简化 iOS 键盘超时、设置页滚动 fallback、恢复 readonly guard、添加 MODULE_MAP、修复 CSS 语法错误；commit 0a7abd9
- [2026-04-21 16:45 kimi] 同步最新构建产物到 Vault，diff 验证通过
- [2026-04-21 17:00 kimi] 实现打分页 5 项修复：卡片滑动 touch-action、日期导航 4 列 grid、新增打分项备注输入框、iOS 键盘 380ms fallback、临时事项 textarea 自动增高；commit 670d08e；已同步到 Vault
- [2026-04-21 18:10 codex] 复查 kimi 改动后补 2 个设置页风险修复：避免 `display()` 重复绑定 touch-scroll guard 监听；补 `touchcancel` 时 readonly 清理；重新构建并同步到 Vault，哈希校验通过
- [2026-04-21 18:35 codex] 按用户反馈收口日期导航：移除左侧原生日期输入，仅保留右侧日历按钮作为日期选择入口；顶部改为只读日期显示块，避免 iPhone 原生日期文本溢出卡片；重新构建并同步到 Vault，哈希校验通过
- [2026-04-21 19:10 codex] 按用户新清单继续做移动端恢复：关闭主打分页 modal 的整页拖动拦截，只保留编辑/导入类卡片的手动上下微调；补 `kid-score-edit-modal` 的实际 transform，使键盘顶起和手动拖动重新生效；设置页新增打分项后滚动到位并自动聚焦名称输入；移除日记 tab 的“填入默认模板”按钮并改成默认自动填充；调整日记成文逻辑，去掉重复的“小记录”段，避免得分页像两篇日记；统一天气/心情和每天小记录在 Mac/iPhone 的布局；自由记录工具栏新增图片/视频/音频 + 加粗/斜体/标题/列表/引用/居中；重新构建并同步到 Vault，哈希校验通过
- [2026-04-22 00:00 codex] 继续收口用户新反馈：iPhone 编辑卡片键盘避让改为按 visualViewport 底边精确贴齐，避免备注变长后把分类和底部操作区一起顶出视野；手动拖动去掉固定 snap 点并改成动态上下边界，允许完整查看卡片最上方和最下方；设置页打分项目列表在新增/删除/重绘时保住当前滚动位置，仅新增时额外滚到新项并聚焦；重新构建并同步到 Vault，哈希校验通过
- [2026-04-22 00:00 codex] 按用户反馈继续优化打分页日记模块：普通“小记录”卡片输入改成自动增高的多行 textarea，不再在卡片内部出现滚动条；天气/心情当前值改成自动增高并支持换行；自定义天气/心情输入区改成更宽松的两行布局，长文本在桌面和 iPhone 上都更容易看全；重新构建并同步到 Vault，哈希校验通过
- [2026-04-22 00:00 codex] 继续优化设置页日记模块编辑器：给模块配置补上 emoji 字段和 emoji picker，新增模块默认带 emoji；提示文案输入改成更大的多行自增高 textarea；同时让打分页里的模块标题显示设置好的 emoji；重新构建并同步到 Vault，哈希校验通过
- [2026-04-22 00:00 codex] 按用户要求清理页面文案：移除所有硬编码的“小朋友”字样，设置页标题改为“每日记录设置”，姓名设置项改为“姓名”，默认回退名称改为“未命名”；重新构建并同步到 Vault，哈希校验通过

## 待 review 重点（给 Codex）

1. `containerEl.scrollTop` 在 Obsidian `PluginSettingTab` 上是否真的有效（可能实际滚动容器是父级 `.vertical-tab-content`）
2. `readonly` guard 会不会误伤正常输入（比如用户想先滚到某个 input 再点击输入——当前实现是 touchend 后 250ms 才解除，可能需要缩短或改成 "touchend 时立即解除但第一次 tap 不触发 focus"）
3. `setupMobileKeyboard` 里 `popup.style.maxHeight` 写死 `vv.height - 32`，在横屏 / 小屏是否合理
4. 桌面端是否还会触发 `touchstart` 相关逻辑（某些触屏笔记本会）
