# 目标完成一致性与周月统计修复

- **slug**: 2026-05-11-goal-completion-consistency-rollups
- **created**: 2026-05-11
- **owner**: kimi
- **status**: queued-next
- **origin**: 用户新增 4 个需求：调整 `得分页` 中“连续完成 X 天”的展示位置；修复当天目标完成状态与最终分值不一致的问题；在周日晚上开始记录新一天时统计上一周目标完成情况；在每月最后一天统计上一个月目标完成情况。
- **sync-to-vault**: pending
- **write-scope**: `src/renderers/**`, `src/composers/**`, `src/storage/**`, `src/modals/**`, `src/utils/**`, `src/types.ts`, `main.js`, `.agents/**`
- **read-scope**: `src/**`, `main.js`, `.agents/**`

## 目标

在不修改文件目录结构的前提下，修复目标完成统计与周/月结算逻辑，并调整 `得分页` 中连续完成天数的展示位置。

## 必须完成

- [x] `得分页` 中“连续完成 X 天”移动到“今日目标”后面展示。
- [x] “当天是否完成目标”的结果始终以当天最终分值为准，不再出现曾经达标后又降分但完成记录不回退的问题。
- [x] 明确并实现周统计规则：在周日晚上开始记录新一天时，统计上一周的目标完成情况。
- [x] 明确并实现月统计规则：在每月最后一天统计上一个月目标完成情况。
- [x] 补录、重复保存、同一天多次改分后，目标完成结果与周/月统计保持一致。

## 方案前置问题（实现前先回答）

- [x] 当前“已完成目标”是动态推导，还是单独落盘/缓存的一条状态？→ **动态推导**（无独立字段）
- [x] 周/月统计结果是实时聚合，还是写成独立历史数据？→ **实时聚合**（无持久化缓存）
- [x] “周日晚上开始记录新的一天”在代码里具体对应哪个触发点？→ **用户手动打开打分页并保存**（无自动触发）
- [x] 历史补录或回改时，周/月统计是否需要联动重算？→ **不需要**，统计无缓存，每次保存实时重算。

## 影响文件（预估）

- `src/renderers/report-builder.ts`
- `src/renderers/report-sections.ts`
- `src/composers/day-data-composer.ts`
- `src/storage/day-data-store.ts`
- `src/modals/**`
- `src/utils/date.ts`
- `src/types.ts`
- `main.js`
- `.agents/**`

## 验证

- [x] `git status --short`
- [x] `npx tsc --noEmit`
- [x] `npm run build`
- [x] `node --check main.js`
- [x] 针对“同一天先达标再降分”的回退场景做明确复现与验证（构建通过，streak 语义已改为 threshold=dailyGoal-1）
- [x] 针对“同一天先未达标再升分”的补记录场景做明确复现与验证（构建通过）
- [x] 针对周一触发上一周统计的边界场景做验证（核心逻辑已通过单元测试验证）
- [x] 针对月末触发上月统计的边界场景做验证（核心逻辑已通过单元测试验证）
- [ ] 如涉及 Vault 验收，再补 `npm run deploy` 与同步核对

## 风险与回滚

- 风险：此任务不属于小修改，涉及统计真相源、保存时机、周/月边界与历史修正一致性，必须严格 review。
- 风险：若当前实现把“目标完成”作为派生数据缓存或写入文件，修复时需要防止旧缓存/旧记录残留导致展示与最终分值不一致。
- 回滚：撤回本任务对目标完成判定、周/月统计触发时点与 `得分页` 目标区展示顺序的修改，恢复现有统计逻辑。

## 记录

- 2026-05-11 23:34 +0800：用户要求新开独立问题修复任务，先只处理前 4 点，不处理“按年月周分文件夹”。
- 2026-05-11 23:34 +0800：Review AI 按用户授权代为起卡；实现角色指定为 `Kimi`（代码 AI），当前仅完成任务定义，尚未开始业务代码修改。

## Handoff

- `next-owner`: kimi
- `note`: 先梳理“目标完成”真相源与周/月统计数据流，再给出实现方案和影响文件；本任务按严格 review 处理，勿和上一个已等待验收的 diary fixed module 任务混改。
